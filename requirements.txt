# =============================================================
# GPU Telemetry TFDV Validation — Dependencies
# =============================================================
# Due to TFDV's strict version constraints, we recommend
# following the step-by-step install in README.md rather
# than using this file directly with pip install -r.
#
# Requires: Python 3.10, Linux x86_64
# =============================================================

# Core
tensorflow==2.15.0

# TFDV stack (install with --no-deps, see README)
tensorflow-data-validation==1.15.1
tensorflow-metadata==1.15.0
tfx-bsl==1.15.1

# TFDV pinned dependencies
absl-py>=0.9,<2.0.0
pandas>=1.0,<2
pyarrow>=10,<11
protobuf>=3.20.3,<4.21
pyfarmhash>=0.2.2,<0.4
joblib>=1.2.0

# Preprocessing & Embeddings
scikit-learn>=1.3
numpy>=1.23,<2.0

# Notebook
nbformat>=5.0
jupyter>=1.0